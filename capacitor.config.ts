import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sakhicollections.app',
  appName: 'Sakhi Collections',
  webDir: 'dist'
};

export default config;
